# Commands Used

FastQC was executed on the raw paired-end sequencing reads using the following command:

```bash
fastqc *.fastq.gz -o results/01_fastqc
```

This command generated quality assessment reports for all FASTQ files in the input directory.
